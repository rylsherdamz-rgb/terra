import { Text, View } from "react-native"
import { useSafeAreaInsets } from "react-native-safe-area-context"

// app header

export default function AppBar() {
    return (<View  className="w-full h-20 border rounded-2xl bg-blue-200">

    </View>)
}